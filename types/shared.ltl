// types/shared.ltl — shared type aliases for ltl_commit_lint

module ltl_commit_lint::types

pub type Id = int
pub type Name = str
pub type Payload = list<int>

pub enum Status {
    Ok,
    Pending,
    Failed { reason: str },
}

pub struct Meta {
    id: Id,
    name: Name,
    status: Status,
}