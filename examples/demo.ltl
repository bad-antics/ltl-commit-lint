// examples/demo.ltl — runnable demo for ltl_commit_lint

import std::io
use ltl_commit_lint as lib

fn main() {
    io::println("ltl_commit_lint demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}